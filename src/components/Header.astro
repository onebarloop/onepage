---
import Nav from './Nav';
---

<script>
  const nav = document.querySelector('.nav');
  const button = document.querySelector('.button');

  button?.addEventListener('click', () => {
    nav?.classList.add('max-w-64');
  });
</script>

<header class="w-full h-header fixed font-vt">
  <Nav client:load />
</header>

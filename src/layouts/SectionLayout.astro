---
const { id, class: className } = Astro.props;
import { cn } from "../lib/utils/cn";
---

<section id={id} class:list={cn("min-h-svh", className)}>
  <div class="container mx-auto pt-8">
    <slot />
  </div>
</section>
